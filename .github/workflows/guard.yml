name: Guardrails

on:
  pull_request:

jobs:
  guard:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Block test tooling
        run: |
          if grep -r "vitest" .; then
            echo "Vitest not allowed in website repo"
            exit 1
          fi
